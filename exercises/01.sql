-- Exercises for tutorial 01 — the shape of a SELECT
-- Write your query under each marker (finish it with a semicolon), then run:
--     python3 check.py 1
-- `ordered` on a marker means row order is part of the answer.

-- @ex 1.1 ordered
-- Name, city and segment of every customer in Germany, sorted by name.

-- @ex 1.2 ordered
-- customer_id, name, signup_date of customers who signed up during 2023,
-- oldest signup first.


-- @ex 1.3 ordered
-- Name and unit_price of products that are NOT discontinued and cost
-- from 5 to 20 inclusive, most expensive first.

-- @ex 1.4
-- customer_id, name, city of the customers whose country is unknown.

-- @ex 1.5 ordered
-- The distinct countries customers come from, excluding unknown, alphabetically.

-- @ex 1.6 ordered
-- name, unit_price and a price_band column for every product:
--   'budget' under 5, 'mid' under 20, 'premium' otherwise. Cheapest first.

-- @ex 1.7 ordered
-- customer_id, name, and country for every customer, but show unknown
-- countries as the text 'Unknown'. Sorted by customer_id.


-- @ex 1.8 ordered
-- The three most expensive products that are still sold (not discontinued):
-- name and unit_price.


-- @ex 1.9 ordered
-- Name and email of customers whose email is NOT an example.com address,
-- sorted by name.
